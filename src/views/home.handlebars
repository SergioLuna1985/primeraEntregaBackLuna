<h1>Productos</h1>

{{#if haveProducts}}
    {{#each products}}
        <div>
        <h2>{{this.title}}</h2>
        <p>Descrición: {{this.description}}</p>
        <p>Stock: {{this.stock}}</p>
        <p>Precio: {{this.price}}</p>
        </div>
    {{/each}}
{{else}}
    <h2>Bienvenido</h2>
{{/if}} 
